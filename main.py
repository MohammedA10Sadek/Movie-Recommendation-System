import start
acc = input('type "sign in" if yoy have an account or "guest" to continue as a guest with limited features \n')
start.user_acc(acc)
# this is the main function that called user_acc from start file to initiate the program
